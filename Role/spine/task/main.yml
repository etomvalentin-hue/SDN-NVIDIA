# roles/spine/tasks/main.yml
---
- include_role: 
    name: common
- include_role:
    name: interface
  vars:
    physical_interfaces: "{{ spine_physical_interfaces }}"
- include_role:
    name: bgp
  vars:
    bgp_config: "{{ spine_bgp_config }}"